/// Support for working with the normal probability distribution.
///
/// `normal` is a small and simple library that provides methods for
/// Gaussian density, cumulative probability, quantiles and random
/// number generation via an abstract class `Normal`, which contains
/// static methods `pdf`, `cdf`, `quantile` (inverse cdf) and `generate`.
///
library normal;

export 'src/normal_base.dart';
